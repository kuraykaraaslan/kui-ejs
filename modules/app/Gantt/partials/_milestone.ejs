<%
  // ── Milestone partial (TODO M4 stub) ────────────────────────────────
  // Locals: task, left, top
  // TODO M4: render a 16x16 diamond rotated 45deg in bg-primary at the start
  // anchor. For now we render nothing so the M1 surface stays clean.
%>
