/* ── KanbanBoard WIP limit (M2 stub) ─────────────────────────────────── */
// TODO M2: read column.wipLimit, render badge "3/5" + flip header tint to red
// when current >= limit. Block dnd drop on over-limit columns via canMove.
//
// Sketch:
//   function updateWipBadges() {
//     // for each column: compute current count, compare to limit, toggle
//     // .text-error class on .kb-col-count, append " / <limit>" suffix.
//   }
