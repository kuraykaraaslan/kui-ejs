<%
  // EmptyState — parallel of parts/EmptyState.tsx.
  // Locals: title, message.
%>
<div class="flex flex-col items-center justify-center text-center px-6 py-10 gap-3">
  <div class="flex items-center justify-center w-12 h-12 rounded-full bg-surface-overlay">
    <i class="fa-solid fa-bell w-5 h-5 text-text-secondary" aria-hidden="true" style="font-size:1.25rem"></i>
  </div>
  <p class="text-sm font-semibold text-text-primary"><%= title %></p>
  <p class="text-xs text-text-secondary leading-snug max-w-[22ch]"><%= message %></p>
</div>
