// modules/ui/ColorPicker/scripts/gradient.js
// TODO M3: multi-stop linear/radial gradient editor controller.
(function () {
  if (window.KuiColorGradient) return;
  window.KuiColorGradient = {
    /** TODO M3: build a `linear-gradient(...)` / `radial-gradient(...)` string. */
    build: function (_stops, _mode) { return ''; },
  };
})();
