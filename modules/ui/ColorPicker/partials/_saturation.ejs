<%#
  modules/ui/ColorPicker/partials/_saturation.ejs

  TODO M2: SV picker pad (saturation × value) driven by the active hue.
  Currently empty — included as a placeholder so the partial graph mirrors
  the NextJS parts/ directory.
%>
