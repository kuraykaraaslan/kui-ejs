<%# Backwards-compatible shim — the implementation now lives in
    `modules/ui/TreeView/`. Existing `include('modules/ui/TreeView', ...)`
    keeps working unchanged. %>
<%- include('./TreeView/TreeView', locals) %>
