# Roadmap

**Stack:** Node.js 20+ · Express 4 · EJS 3 · TypeScript 5 · Tailwind CSS 4 · PostCSS

| No | Feature / Module | Description | Why Needed | EJS / Express Implementation Notes | Priority | Coverage |
|----|-----------------|-------------|------------|-------------------------------------|----------|----------|
| 1 | **Partial Variant System** | Each EJS partial accepts a `variant`, `size`, and `intent` local and maps it to a class string via a `const variantClasses` object at the top of the partial | Prevents scattered one-off className strings duplicated across themes; enforces token-aligned visual consistency | Define `modules/ui/Button.ejs` to expect `locals.variant` (default `'primary'`); top of file: `<% const v = variant \|\| 'primary'; const cls = {primary:'bg-primary text-primary-fg', secondary:'...'} %>` then `class="<%= cls[v] %>"` | Must-have | ✅ Complete — tüm `modules/ui/` partial'larında `_v`/`_sz`/`vc`/`sc` lookup tabloları standartlaştırıldı; `bg-surface-active`, `text-text-tertiary`, `ring-primary/20`, `hover:border-text-tertiary` gibi sahte token kullanımları token-aligned karşılıklarıyla değiştirildi |
| 2 | **Polymorphic Element Override** | EJS partials accept an `element` local (`'a'`, `'button'`, `'div'`) so the same partial renders as different HTML elements without wrapper markup | Reuse `Button.ejs` as an anchor when `href` is provided, without a second template | In `Button.ejs`: `<% const el = element \|\| 'button'; %>` then `<<%= el %> ...attrs...>` — pass `element: 'a'`, `href: '/path'` from the include call | Must-have | ✅ Complete — `Button.ejs` `element` local'ı ile `<a>`, `<button>` vb. render ediyor; `href`, `target`, `rel`, `aria-disabled` otomatik yönetiliyor |
| 3 | **CSS Variable Token Contract** | All design tokens declared in `public/css/input.css` under `:root` and `.dark {}`, used only as Tailwind utilities (`bg-primary`, `text-text-secondary`) — never raw hex in views | Single source of truth; prevents drift between Tailwind utility values and raw hex values | ✅ Tamamlandı — `input.css` tüm semantic token'ları `@theme inline` ile tanımlıyor | Must-have | ✅ Complete — token'lar tanımlı; `promotional.ejs` içindeki `#3b82f6`/`#8b5cf6` gradient'lar `var(--primary)`/`var(--secondary)` ile değiştirildi; `scripts/audit-tokens.sh` CI'a eklendi (`npm run audit:tokens`); email/OAuth/kart/Leaflet allowlist belgelendi |
| 4 | **Runtime Theme Switching** | Dropdown in the showcase topbar reads/writes `localStorage.theme` (`'light'`, `'dark'`, `'system'`) and toggles `.dark` on `<html>`; persists across page navigations | Multi-tenant demos need runtime branding without a server round-trip | ✅ Tamamlandı — `views/showcase/partials/theme-switcher.ejs` + `showcase/index.ejs` inline script | Must-have | ✅ Complete — `theme-switcher.ejs` + `localStorage` toggle mevcut |
| 5 | **Dark Mode Provider Script** | Inline `<script>` injected before first paint reads `localStorage.theme`, applies `.dark` class to `<html>` before CSS loads to prevent flash-of-wrong-theme | Critical for UX — without it dark-mode users see a white flash on every page load | ✅ Tamamlandı — inline script `showcase/index.ejs` içinde `localStorage` + `prefers-color-scheme` okuyor | Must-have | ✅ Complete — inline anti-flash script paint öncesi çalışıyor |
| 6 | **Component Metadata Registry** | `src/data/showcase.menu.ts` holds every component with `id`, `title`, `category`, `abbr`; `src/data/showcase.data.ts` holds full `ShowcaseItem` with `filePath`, `sourceCode`, `variants` | Sidebar, search, URL routing, and status badges all derive from one source with no duplication | `ShowcaseNavItem` tipine `status: 'stable' \| 'beta' \| 'deprecated'` ve `since: string` alanları ekle; `src/types/index.ts` üzerinden güncelle | Must-have | ✅ Complete — `status?: ComponentStatus` ve `since?: string` `src/types/index.ts`'de tanımlı; `showcase.menu.ts` tüm item'larda `status`/`since` dolu; #19 ile sidebar'da badge olarak render ediliyor |
| 7 | **Server-Rendered Variant Grid** | Showcase route renders a `layout=grid` view that outputs all variants of a component simultaneously in a CSS grid — no client-side toggle needed | Designers need to see all states at once; server rendering means it works without JS | `ShowcaseVariant` tipine `layout?: 'side' \| 'stack' \| 'grid'` ekle; `showcase/index.ejs` içinde grid layout branch'i yaz; route `?layout=grid` query param'ını okusun | Must-have | ✅ Complete — `?layout=grid\|side\|stack` query param; `src/routes/index.ts` `variantLayout` local olarak geçiyor; `widget.ejs` grid modda compact preview-only cell render ediyor; `topbar.ejs`'de 3 layout toggle butonu |
| 8 | **Source Code Viewer with Copy** | Collapsible block showing the EJS include snippet for each variant with a copy-to-clipboard button | Developers copy real usage code, not pseudocode | ✅ Tamamlandı — `views/showcase/partials/source-block.ejs` (full source) + `widget.ejs` (per-variant code panel) `copy-btn` ile | Must-have | ✅ Complete — `source-block.ejs` + `widget.ejs` copy + expand/collapse mevcut |
| 9 | **Domain Locals Contract** | TypeScript interface per domain partial exported from `modules/<layer>/<Component>.locals.ts`; routes must satisfy the interface before calling `res.render()` | Domain partials shared across themes must agree on expected locals; catches missing data at compile time | `modules/ui/Button.locals.ts` oluştur: `export interface ButtonLocals { variant?: 'primary'\|'secondary'; label: string; element?: string }` — route'lar bu interface'i import edip typecheck'ten geçirsin | Must-have | 🟡 Partial — `src/types/index.ts` `ShowcaseItem`/`ShowcaseVariant` tanımlıyor; per-partial locals interface'leri yok |
| 10 | **Form System with Server-Side Validation** | Every form partial posts to an Express route that validates the body with Zod before rendering the response; validation errors are passed back as `locals.errors` | Without a validation contract each form reinvents error handling; server-rendered forms must validate on the server | `zod` bağımlılığını ekle; `src/middleware/validateBody.ts` oluştur: Zod schema'yı wrap eder, `next()` ya da `{errors}` ile re-render yapar; tüm form POST route'larına bağla | Must-have | 🟡 Partial — `zod ^3.23.0` eklendi; `src/middleware/validateBody.ts` oluşturuldu (success/failure path, `res.locals.errors`, `res.locals.values`); form POST route örnekleri ve views henüz yok |
| 11 | **Accessibility Audit Script** | `npm run a11y` script that spawns the dev server, fetches each registered showcase URL with `axe-playwright`, and prints pass/fail per partial | Catch missing `aria-label`, broken focus order, low contrast before production; WCAG 2.1 AA requirement | `@axe-core/playwright` devDependency olarak ekle; `scripts/a11y-audit.ts` oluştur — `SHOWCASE_DATA` ID'lerini iterate eder, her route için `axe.analyze()` çalıştırır; JSON rapor çıktısı | Must-have | ❌ Yok — `axe-core` bağımlılığı yok |
| 12 | **Focus Trap Script** | Vanilla JS function `trapFocus(containerEl)` in `public/js/main.js` that constrains Tab/Shift+Tab within a container; called by Modal and Drawer on open | Required for `Modal.ejs` and `Drawer.ejs` to be keyboard-accessible; without it Tab escapes the overlay | `public/js/main.js` içine `function trapFocus(el) { ... }` ekle; Modal/Drawer açılışta `trapFocus(modalEl)` çağırsın, kapanışta `releaseFocus()` | Must-have | ✅ Complete — `ModalModule` in `main.js`: Tab/Shift+Tab döngüsü, Escape kapanışı, tetikleyiciye focus dönüşü implement edildi; `window.openModal` / `window.closeModal` global API |
| 13 | **ARIA Live Region Partial** | Shared partial `views/partials/liveRegion.ejs` renders a hidden `<div role="status" aria-live="polite" id="live-region"></div>`; a JS helper `announce(msg)` writes to it | Screen readers miss async state changes (form submit, toast, filter apply) without explicit announcements | `liveRegion.ejs` her layout'a bir kez include edilsin; `public/js/main.js` içinde `function announce(msg, politeness='polite')` export edilsin; Toast partial `announce()` çağırsın | Must-have | ✅ Complete — `AnnounceModule` `main.js`'e eklendi; `#live-region` DOM'a mount ediliyor (`role="status"`, `aria-live="polite"`, `class="sr-only"`); `window.announce(message, politeness?)` global API; 50ms clear→set döngüsü tüm AT implementasyonlarında güvenilir mutation garantisi |
| 14 | **Responsive CSS Utilities Policy** | Document and enforce a CSS-only responsive pattern using Tailwind's `sm:`, `md:`, `lg:` prefixes as the sole breakpoint mechanism; `window.matchMedia()` only as a last resort | EJS is always SSR — no hydration mismatch; Tailwind utilities handle 99% of responsive cases without JS | Mevcut partial'ları `if window.innerWidth >` JS kullanımı için denetle; Tailwind responsive variant'larıyla değiştir; politikayı `AGENTS.md` içinde belgele | Must-have | 🟡 Partial — Tailwind responsive prefix'leri kullanılıyor; yazılı politika yok |
| 15 | **Spacing Scale Enforcement** | ESLint or grep-based CI check that flags arbitrary spacing values (`p-[13px]`, `mt-[5px]`) in `.ejs` files; only Tailwind 4px-grid classes allowed | Prevents pixel-hunting and maintains vertical rhythm | `scripts/lint-spacing.sh` ekle: `grep -rn "p-\[.*px\]\|m-\[.*px\]" views/ modules/ && exit 1`; `npm run build` içine dahil et; `.ts` dosyaları için `eslint-plugin-tailwindcss` ekle | Must-have | ✅ Complete — `scripts/lint-spacing.sh` oluşturuldu; PCRE pattern ile `p-`, `m-`, `gap-`, `space-` utility'lerindeki `[Npx]` değerleri yakalanıyor; `npm run lint:spacing` ile çalışıyor; mevcut codebase'de ihlal yok |
| 16 | **Dead Partial Detection** | Script `scripts/find-dead-partials.ts` that lists every `.ejs` file under `modules/` and `views/partials/` and checks whether it appears in any `include(...)` call across all `.ejs` files | Unused partials accumulate; contributors are confused about what is safe to delete | `find modules/ views/partials/ -name "*.ejs"` → her dosya için `grep -r "include.*<filename>" views/ modules/` → sıfır eşleşen dosyaları raporla | Must-have | ✅ Complete — `scripts/find-dead-partials.sh` oluşturuldu; `npm run dead-partials` ile çalışıyor; 121 partial tarıyor, 44'ü referanssız (intentional stubs); exit 0 ile informational |
| 17 | **Conditional Script Loading per Theme** | Each theme route declares the JS files it needs via a `scripts` local: `res.render(view, { scripts: ['/js/chart.js'] })`; the layout appends those `<script>` tags before `</body>` | A user viewing the auth theme should not download Leaflet or Chart.js; reduces page weight | Layout'a `scripts?: string[]` locals ekle; `views/layouts/blank.ejs` içinde `<% (scripts\|\|[]).forEach(s => { %><script src="<%= s %>"></script><% }) %>`; her theme route sadece kendi script'lerini geçsin | Must-have | 🟡 Partial — her iki layout'ta `<%- locals.extraScripts \|\| '' %>` ile string injection mevcut; ancak `scripts: string[]` array pattern yerine ham HTML string geçiliyor |
| 18 | **Showcase Search & Filter** | Client-side search input in the sidebar header that filters `ShowcaseNavItem` entries by `title`, `category`, and `description` in real time; no server round-trip | With 80+ components, scrolling the sidebar is unusable | `sidebar.ejs` header'ına `<input id="sidebar-search">` ekle; `public/js/main.js` içinde 150ms debounce ile eşleşmeyen `<li>` elemanlarına `display:none` uygula | Must-have | ✅ Complete — `#sidebar-search` input collapse button altına eklendi; `data-nav-item` + `data-search-title` item'larda; `data-group-section` gruplarda; `SidebarSearchModule` showcase.js'de 150ms debounce + Escape clear |
| 19 | **Component Status Badges** | `stable`, `beta`, `deprecated` status field on every `ShowcaseNavItem`; rendered as a colored dot or label in the sidebar and in the component header | Prevents downstream teams from using unstable partials in production | `ShowcaseNavItem` tipine `status?: 'stable' \| 'beta' \| 'deprecated'` ekle; `sidebar.ejs` içinde item title yanına `<span class="..."><%= item.status %></span>` render et | Must-have | ✅ Complete — `beta` → amber "β" badge; `deprecated` → kırmızı strikethrough "dep" badge; `stable` badge yok (gürültüsüz); desktop + mobile sidebar item loop'larında; `aria-label` ile AT uyumlu |
| 20 | **Escaped Output Policy Enforcement** | CI grep script that audits every `<%- ... %>` (raw unescaped) usage in `.ejs` files and fails if the variable is not on an explicit allowlist (`previewHtml`, `sourceCode`, `include(...)`) | Prevents accidental XSS from untrusted data rendered raw; `<%= %>` must be the default | `scripts/audit-raw-output.sh`: `grep -rn "<%- " views/ modules/ \| grep -v "include\|previewHtml\|sourceCode\|variant\.code\|JSON\." && exit 1` | Must-have | 🟡 Partial — `scripts/audit-raw-output.sh` oluşturuldu; `npm run audit:raw` ile çalışıyor; **46 mevcut ihlal** bulundu — bunlar `modules/` içindeki slot/composition pattern'ları (`locals.children`, `col.render(row)`, vb.); allowlist genişletilmeli veya `<%= %>` geçişi yapılmalı |
| 21 | **Partial Props Contract Documentation** | Auto-generated HTML table in the showcase showing expected locals for each partial — field name, type, required/optional, default — derived from `*.locals.ts` TypeScript interfaces | Manually maintained prop docs go stale; teams need to know what to pass to each `include()` call | `scripts/gen-locals-docs.ts` yaz: `ts-morph` ile `*.locals.ts` dosyalarını parse et, JSON çıktısına yaz; showcase route JSON'u okur, `LocalsTable.ejs` partial'ı ile tablo render eder | Must-have | ❌ Yok — locals contract dökümantasyonu yok |
| 22 | **i18n-Ready String Props** | All user-facing strings in UI partials passed as optional locals with English defaults (`<%= label \|\| 'Submit' %>`) rather than hardcoded text | Hardcoded English strings block internationalization; RTL languages also need direction context | `modules/ui/` partial'larını hardcoded string kullanımı için denetle; `<%= locals.submitLabel \|\| 'Submit' %>` formuna dönüştür; her partial'ın `.locals.ts` interface'ine varsayılan değerle ekle | Should-have | 🟡 Partial — `DirectionProvider.ejs` + `LanguageSwitcher.ejs` mevcut; string extraction standartlaştırılmamış |
| 23 | **RTL Layout Support** | Use `start`/`end` Tailwind utilities throughout; `dir` attribute on `<html>` driven by a `locale` local passed from the route; `DirectionProvider.ejs` wraps content sections | Arabic, Hebrew, Persian markets require RTL without layout rewrites | `app.locals` içinde varsayılan `dir: 'ltr'` tanımla; route'lar locale'e göre override etsin; `layouts/blank.ejs` içinde `<html lang="<%= locale %>" dir="<%= dir %>">` | Should-have | 🟡 Partial — `DirectionProvider.ejs` mevcut; layout `dir` attribute'u route locals'a bağlı değil |
| 24 | **Component Changelog** | `changelog?: ChangelogEntry[]` field on `ShowcaseItem`; rendered as a collapsible table below the variant grid showing version, date, and change description | Teams upgrading the design system need to know what broke per component | `src/types/index.ts` içine `ChangelogEntry = { version: string; date: string; note: string }` ekle; `ShowcaseItem`'a `changelog?` alanı ekle; `showcase/index.ejs` içinde source block altında render et | Should-have | ❌ Yok — `ShowcaseItem` tipinde `changelog` alanı yok |
| 25 | **Visual Regression Test Suite** | Playwright test that navigates to each showcase URL (`/:slug`), screenshots the widget area, and diffs against a stored baseline on CI | CSS refactors silently break components that aren't manually verified; screenshots catch it | `@playwright/test` devDependency olarak ekle; `tests/visual.spec.ts` oluştur — `SHOWCASE_DATA` üzerinde loop kurarak `expect(page).toHaveScreenshot(\`${id}.png\`)` çağır | Should-have | ✅ Complete — `playwright.config.ts` + `tests/visual/showcase.spec.ts`/`themes.spec.ts`/`keyboard-trap.spec.ts` (220 tests), plus `@axe-core/playwright` scanning the same page load and a nightly CI workflow ($KUIREACT_ROOT/docs/dev/phase-2-testing.md 2.3–2.5) |
| 26 | **Route Smoke Tests** | Supertest suite that GETs every registered showcase route and theme route, asserts HTTP 200, and checks that the HTML contains the expected `<title>` and no leftover `<%=` EJS tags | Prevents broken routes from reaching production; EJS render errors return 500 silently in some configs | `supertest` + `vitest` devDependency olarak ekle; `tests/routes.spec.ts` oluştur — `src/app.ts`'yi import et, tüm route path'lerini theme registry'den toplayarak döngüyle test et | Should-have | ✅ Complete — `tests/routes.smoke.test.ts` (supertest + vitest), 676 total tests across `tests/*.test.ts` covering routes, html-validate, security headers, and partial rendering |
| 27 | **Snapshot Tests for Rendered HTML** | Supertest test that renders each `ShowcaseItem` route and compares the HTML output to a stored `.html` snapshot; fails on unexpected changes | Catches unintended template drift; complements visual regression for non-visual changes like ARIA attributes | `tests/snapshots.spec.ts` içinde her `/:slug` route'u GET et; dinamik değerleri (timestamp) çıkar; vitest snapshot ile `expect(html).toMatchSnapshot()` karşılaştır | Should-have | ❌ Yok |
| 28 | **Server-Side Data Adapter Pattern** | Typed adapter interface per domain vertical (`AuthAdapter`, `CartAdapter`) in `src/adapters/<vertical>.ts`; routes import the adapter and pass its output as locals — never data files directly | Theme routes should be decoupled from data source; swapping static fixtures for a real API only requires a new adapter | `interface AuthAdapter { getLoginFormData(): LoginFormData }` tanımla → `src/adapters/auth.ts` implement etsin; route `adapter.getLoginFormData()` çağırsın | Should-have | ❌ Yok — route'lar data dosyalarını doğrudan import ediyor |
| 29 | **Per-Theme Script Bundle** | Each theme route declares needed JS via `scripts: [...]` locals; a `npm run build:js` step uses esbuild to produce one bundle per theme under `public/js/themes/` | Prevents cross-theme JS leakage; modem theme should not ship auth-form validation JS | `esbuild` devDependency olarak ekle; `scripts/build-js.ts` her theme için bir entrypoint tanımlasın; `public/js/themes/<vertical>.js` çıktısı üretsin | Should-have | ❌ Yok — tek global `public/js/main.js` mevcut |
| 30 | **Token Audit Script** | `scripts/audit-tokens.ts` reads all CSS variable declarations from `public/css/input.css` and greps `views/` + `modules/` for each token name; outputs a list of unused tokens | Orphaned tokens accumulate and confuse contributors; catches token name typos | `:root { ... }` bloğunu regex ile parse et, `--token-name` listesini çıkar; her isim için `grep -r "var(--token-name)" views/ modules/ public/js/` çalıştır; sıfır eşleşenleri raporla ve CI'da fail et | Should-have | ❌ Yok — `scripts/` dizini yok |
| 31 | **EJS Include Dependency Graph** | Script `scripts/dep-graph.ts` that scans all `.ejs` files, extracts `include(...)` calls, and builds a directed graph; outputs JSON and optionally a Mermaid diagram | Prevents circular includes (which crash EJS); shows which partials are most reused vs orphaned | `include\(['"]([^'"]+)['"]\)` regex ile kenarları çıkar; DFS ile döngü tespiti yap; `scripts/dep-graph.mmd` dosyasına aktar | Should-have | ❌ Yok — bağımlılık grafı aracı yok |
| 32 | **Animation Token System** | Named animation tokens in `public/css/input.css`: `--duration-fast: 100ms`, `--duration-base: 200ms`, `--duration-slow: 350ms`, `--ease-out: cubic-bezier(0.16,1,0.3,1)` | Inconsistent animation speeds make the UI feel jittery; centralizing them makes speed-feel tunable globally | `:root {}` bloğuna değişkenleri ekle; Tailwind `@theme` bloğuyla map et; token audit scriptiyle (item #30) sahipsiz kalmaları engelle | Should-have | ❌ Yok — birçok partial'da `transition: 200ms` hardcoded |
| 33 | **Reduced Motion CSS Support** | `@media (prefers-reduced-motion: reduce)` block in `public/css/input.css` that overrides all `transition-*` and `animation-*` properties to `0ms` | Legal requirement in some jurisdictions; critical for users with vestibular disorders | `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { transition-duration: 0.01ms !important; animation-duration: 0.01ms !important; } }` ekle | Should-have | ❌ Yok — reduced-motion CSS veya JS desteği yok |
| 34 | **Showcase URL State Sync** | Active component slug already in the URL (`/:slug`); additionally sync dark mode preference and active variant index as query params (`?dark=1&v=2`) so the URL is fully shareable | Bug reporters need to paste a URL that opens the exact component variant they saw | `public/js/main.js` içinde theme/variant değişiminde `history.replaceState(null, '', '?' + new URLSearchParams({dark, v}))` çağır; sayfa yüklenişinde param'ları paint öncesi uygula | Should-have | 🟡 Partial — slug routing mevcut; dark/variant query param sync yok |
| 35 | **Multi-Theme CSS Token Override** | Dropdown in showcase topbar that writes `data-theme="ocean"` to `<html>`; `[data-theme="ocean"] { --primary: #06b6d4; ... }` blocks in `input.css` demonstrate white-label capability | Shows that the token system supports theming without code changes; modem vs common themes can have distinct brand colors | `input.css` içine `[data-theme="ocean"] {}` ve `[data-theme="brand"] {}` blokları ekle; `theme-switcher.ejs` dropdown'una seçenekler ekle; topbar attribute'u yazar, localStorage'a persist eder | Should-have | 🟡 Partial — dark/light/system toggle mevcut; named CSS theme varyantları yok |
| 36 | **Multi-Page Domain Example Flows** | Multi-page walkthrough routes showing component composition in real user journeys: auth flow (login → 2FA → verify), checkout flow (cart → payment → confirmation), modem flow (dashboard → wifi → parental) | Isolated component previews do not show how components interact in a real user journey | ✅ Tamamlandı — `views/theme/modem/` (13 sayfa), `views/theme/common/auth/` (6 sayfa), `views/theme/common/cart/`, `views/theme/common/payment/`, `views/theme/api-doc/` | Should-have | ✅ Complete — modem, api-doc, common çok sayfalı flow'larla mevcut |
| 37 | **Loading State Partials** | Each domain partial that shows data has a `state: 'loading'` local variant using skeleton shimmer via CSS animation | SSR pages can still show skeleton states during long server operations or htmx partial updates | `<% if (state === 'loading') { %> <%- include('../../ui/Spinner') %> <% } else { %> ... real content ... <% } %>` — `modules/app/LoadingState.ejs` mevcut; domain partial'larına sistematik olarak bağlanacak | Should-have | 🟡 Partial — `modules/app/LoadingState.ejs` mevcut; domain partial'larına bağlı değil |
| 38 | **Per-Theme Error Pages** | `views/theme/<vertical>/500.ejs` and `views/theme/<vertical>/404.ejs` for each theme; Express error middleware renders the theme-matched error page | Generic error pages break the theme illusion; modem dashboard should show a modem-styled error | `app.ts` error handler içinde `req.path`'ten theme'i türet; `res.render(\`theme/${theme}/500\`, ...)` dene, fallback olarak generic `views/404.ejs` kullan | Should-have | ❌ Yok — tek global `views/404.ejs` mevcut; `500.ejs` ve per-theme error routing yok |
| 39 | **Server-Side Request Analytics** | Express middleware that logs `{route, component, timestamp}` to a local NDJSON file on each showcase route hit; toggled by `ANALYTICS=1` env var | Tells the maintainer which components are actually viewed vs ignored without third-party trackers | `src/middleware/analytics.ts` oluştur; `process.env.ANALYTICS === '1'` ise `app.use(analyticsMiddleware)` ekle; `logs/access.ndjson` dosyasına yaz | Nice-to-have | ❌ Yok |
| 40 | **Figma Token Sync Script** | `scripts/sync-tokens.ts` reads Figma Variables REST API and writes updated `--token` values into the `:root {}` block in `public/css/input.css` | Design tokens drift between Figma and code when updated manually | `node-fetch` kullan; Figma API response'unu parse et; `input.css` içindeki `:root { ... }` bölümünü regex ile değiştir; `npm run sync-tokens` ile çalıştır | Nice-to-have | ❌ Yok |
| 41 | **Shareable Showcase URL** | Encode the active variant index and layout preference as base64 query params (`?state=eyJ2IjoyfQ==`) so a specific component configuration can be linked | Designers sharing exact configurations with engineers without needing screenshots | `public/js/main.js` içinde variant değişiminde `btoa(JSON.stringify({v: variantIndex, layout}))` → `URLSearchParams`; sayfa yüklenişinde decode edip uygula | Nice-to-have | ❌ Yok |
| 42 | **Server-Sent Events for Toast** | Express endpoint `GET /events` using `text/event-stream`; server pushes toast notifications to connected clients; `public/js/main.js` creates `EventSource` and calls `showToast()` on receipt | Cross-domain state changes (auth expiry, server maintenance) need to reach the UI without polling | `src/routes/index.ts` içine `router.get('/events', (req, res) => { res.setHeader('Content-Type','text/event-stream'); ... })` ekle; `Toast.ejs` partial server-pushed mesajları render etsin | Nice-to-have | ❌ Yok |
| 43 | **Automated A11y Report Route** | Express route `GET /showcase/a11y-report` that renders a page listing every registered component with axe-core pass/fail status; data generated by `scripts/a11y-audit.ts` and written to `public/a11y-report.json` | Regulatory compliance requires documented evidence of accessibility testing | Audit scriptini CI'da çalıştır; sonucu `public/a11y-report.json`'a yaz; showcase route JSON'u okur, `views/showcase/a11y-report.ejs` ile pass/fail badge'leriyle render eder | Nice-to-have | ❌ Yok |
| 44 | **Locals API Diff Tool** | CLI script `scripts/diff-locals.ts` that compares `*.locals.ts` interface exports between two git refs and reports added, removed, or renamed fields | Downstream theme routes break silently when a partial's expected local is renamed; teams need advance warning | `ts-morph` kullan; `git show <ref>:<file>` ile iki versiyonu al; export edilen interface member isimlerini ve tiplerini diff'le; breaking change'leri tablo olarak çıktıla | Nice-to-have | ❌ Yok |
| 45 | **Helmet + CSP Headers** | `helmet` Express middleware applied in `app.ts` with a strict `Content-Security-Policy` allowing only `self`, `cdnjs.cloudflare.com`, `unpkg.com`, and `cdn.jsdelivr.net`; inline dark-mode script tagged with a per-request nonce | Protects against XSS injection; required for any production deployment; no security headers currently set | `npm install helmet`; `app.ts` içinde `app.use(helmet({ contentSecurityPolicy: { directives: { scriptSrc: ["'self'", "cdnjs.cloudflare.com", "'nonce-<%= nonce %>'"] } } }))`; her request için nonce üret | Must-have | 🟡 Partial — `helmet` eklendi ve aktif; ancak `contentSecurityPolicy: false` ile CSP devre dışı bırakıldı (CDN varlıkları nedeniyle); diğer helmet başlıkları (HSTS, X-Frame-Options vb.) çalışıyor |
| 46 | **Static Asset Versioning** | CSS and JS `<link>`/`<script>` tags include a content hash query param (`/css/main.css?v=abc123`) computed at build time and injected into `app.locals.assetVersion` | Without cache busting, browsers serve stale CSS after a redeploy | `build:css` script'inde `public/css/main.css`'i `crypto.createHash('md5')` ile hashle; `public/asset-manifest.json`'a yaz; `app.ts` `app.locals.assetVersion` olarak okur; layout'lar `href="/css/main.css?v=<%= assetVersion %>"` kullanır | Must-have | ✅ Complete — `app.locals.assetVersion` startup'ta `crypto.createHash('md5')` ile `app.css` içeriğinden 8 char hash olarak hesaplanıyor; `_head.ejs` + `showcase/index.ejs` CSS link'i `?v=<%= assetVersion %>` ile güncellendi; dev fallback `'dev'` |
| 47 | **Raw HTML Allowlist** | Documented list of every `<%- ... %>` (unescaped) usage site with a justification comment; enforced by the audit script from item #20 | `<%- %>` is the XSS vector in EJS; every usage must be intentional and reviewed | `docs/raw-output-allowlist.md` oluştur: her dosya/satır için gerekçe listele (`previewHtml` — developer-controlled, `sourceCode` — pre-escaped, `include(...)` — always safe); CI script listenin gerçek kullanımla örtüştüğünü doğrulasın | Must-have | ✅ Complete — `docs/raw-output-allowlist.md` oluşturuldu; 58 raw output sitesi kategorilendirildi (developer-controlled, body placeholder, slot/composition, pre-escaped, structured data); 255 `include()` çağrısı listelendi; CI enforcement bölümü eklendi |
| 48 | **Server-Side Flash Messages Partial** | `views/partials/flash.ejs` renders `locals.flash` set by `connect-flash` or a cookie-based middleware; layouts include it once near the top of `<body>`; routes set flash before redirecting | PRG (Post-Redirect-Get) pattern requires flash storage to survive the redirect; form submissions need user feedback | `connect-flash` + `express-session` ekle (ya da basit cookie-based flash yaz); `views/partials/flash.ejs` her iki layout'a dahil et; route `res.locals.flash = {type, message}` set edip `res.redirect()` çağırsın | Should-have | 🟡 Partial — `views/partials/_flash.ejs` oluşturuldu; her iki layout'a dahil edildi; `flash.type` / `flash.message` locals ile çalışıyor; ancak redirect'te flash'ı yaşatan `connect-flash` / session middleware yok |
| 49 | **Build Pipeline CI Validation** | `npm run ci` script that runs CSS compile, TypeScript type-check, dead partial detection, raw output audit, token audit, and route smoke tests in sequence; fails fast on first error | Contributors who only test locally miss TypeScript or CSS errors that break the deployed build | `package.json` içine `"ci": "npm run build && npm run test && npm run lint-spacing && npm run audit-tokens && npm run audit-raw"` ekle; her sub-script non-zero exit ile fail etsin | Must-have | ✅ Complete — `npm run ci` → build + audit:tokens + audit:raw + lint:spacing + dead-partials zinciri; `npm run audit:raw`, `npm run lint:spacing`, `npm run dead-partials` ayrı ayrı çalıştırılabilir; `zod ^3.23.0` bağımlılığı eklendi |
| 50 | **Contribution Conventions Docs** | `CONTRIBUTING.md` documenting: file naming conventions, the four-layer architecture, EJS include path rules, the `res.render()` locals pattern, how to add a new partial, and the `<%= vs <%- ` safety policy | Without written conventions, contributors duplicate partials, hardcode data in views, or introduce raw output in wrong places | `AGENTS.md` içindeki kuralları developer-friendly `CONTRIBUTING.md`'ye taşı; yeni atom, molecule ve theme route ekleme için işlenmiş örnekler ekle | Should-have | ❌ Yok — `AGENTS.md` mimariyi kapsamakta; contribution workflow dökümantasyonu yok |

---

## Summary

| Status | Count |
|--------|-------|
| ✅ Complete | 20 |
| 🟡 Partial | 12 |
| ❌ Not implemented | 18 |

**Critical gaps (Must-have + ❌):** Accessibility Audit Script · Partial Locals Contract · Spacing Enforcement Policy (doc) · Route Smoke Tests

**Must-have / Partial (kalan iş var):** Form System (routes/views eksik) · Escaped Output Audit (46 ihlal allowlist'e eklenmeli) · Conditional Script Loading (array pattern) · Helmet CSP (aktif etmek gerekiyor)

---

## Parallel Map: Next.js ↔ EJS

| EJS Item | Next.js Item | Relationship |
|----------|-------------|--------------|
| #1 Partial Variant System | #1 Variant System | Direct equivalent — `variantClasses` object at top of `.ejs` instead of `Record<Variant, string>` in `.tsx` |
| #2 Polymorphic Element Override | #2 Polymorphic `as` Prop | Direct equivalent — `element` local instead of React generic |
| #3 CSS Variable Token Contract | #3 CSS Variable Token Contract | Identical — same token names, same `:root` structure |
| #6 Component Metadata Registry | #6 Component Metadata Registry | Direct equivalent — add `status` + `since` to `ShowcaseNavItem` |
| #7 Server-Rendered Variant Grid | #21 Variant Grid View | Direct equivalent — CSS grid in EJS instead of React layout prop |
| #8 Source Code Viewer | #8 Source Code Viewer with Copy | Identical intent, already complete in both |
| #9 Domain Locals Contract | #9 Domain Component Contracts | Direct equivalent — `.locals.ts` interface instead of React prop type |
| #11 A11y Audit Script | #11 Accessibility Audit Hook | Direct equivalent — Playwright + axe at build time instead of `useEffect` |
| #12 Focus Trap Script | #12 Focus Trap Utility | Direct equivalent — vanilla JS instead of React hook |
| #13 ARIA Live Region Partial | #13 Announce Utility | Direct equivalent — shared partial + `announce()` instead of singleton function |
| #16 Dead Partial Detection | #16 Tree-Shaking Export Strategy | EJS equivalent — filesystem grep instead of bundler analysis |
| #17 Conditional Script Loading | #17 Dynamic Import Wrappers | EJS equivalent — `scripts: []` locals instead of `next/dynamic` |
| #18 Showcase Search | #18 Showcase Search & Filter | Direct equivalent — vanilla JS filter instead of React `useMemo` |
| #19 Component Status Badges | #19 Component Status Badges | Identical — same `stable/beta/deprecated` vocabulary |
| #21 Partial Props Contract Docs | #35 Prop Table Auto-Render | Direct equivalent — `ts-morph` parses `.locals.ts` instead of `react-docgen-typescript` |
| #24 Component Changelog | #24 Component Changelog | Identical — same `ChangelogEntry[]` shape on the metadata type |
| #25 Visual Regression Tests | #25 Visual Regression Test Suite | Identical — same Playwright tooling |
| #26 Route Smoke Tests | #26 Unit Test Harness | EJS equivalent — supertest route tests instead of React Testing Library |
| #28 Data Adapter Pattern | #28 API Integration Adapter Pattern | Direct equivalent — typed adapter interface, injected into routes |
| #30 Token Audit Script | #30 Token Audit Script | Identical concept — grep against `.ejs` + CSS instead of `.tsx` |
| #31 EJS Include Dependency Graph | #31 Component Dependency Graph | Direct equivalent — `include()` call graph instead of JS import graph |
| #32 Animation Token System | #32 Animation Token System | Identical — same token names in the same `input.css` |
| #33 Reduced Motion Support | #33 Reduced Motion Support | Identical — same `@media (prefers-reduced-motion: reduce)` CSS block |
| #34 Showcase URL State Sync | #34 Showcase URL State Sync | Direct equivalent — `history.replaceState()` instead of `useSearchParams` |
| #35 Multi-Theme CSS Override | #36 Multi-Theme Showcase Toggle | Direct equivalent — `data-theme` attribute instead of React context |
| #38 Per-Theme Error Pages | #39 Error Boundary per Route | Direct equivalent — `500.ejs` per theme instead of `error.tsx` |
| #44 Locals API Diff Tool | #45 Component API Diff Tool | Direct equivalent — diff `.locals.ts` interfaces instead of `.tsx` prop types |

### EJS-Specific Items (No Next.js Equivalent)

| Item | Why EJS-only |
|------|-------------|
| #20 Escaped Output Policy Enforcement | `<%- %>` is an EJS-specific XSS vector; React JSX escapes by default |
| #45 Helmet + CSP Headers | Express must set security headers explicitly; Next.js has `next.config.js` headers but no nonce middleware |
| #46 Static Asset Versioning | Express serves static files manually; Next.js fingerprints assets automatically in build output |
| #47 Raw HTML Allowlist | Directly tied to the `<%- %>` EJS tag; no React equivalent |
| #48 Server-Side Flash Messages | PRG pattern + flash storage is Express-specific; React handles this with client-side state |
| #42 Server-Sent Events for Toast | Replaces the React `EventBus` pattern with an HTTP SSE endpoint |
| #49 Build Pipeline CI Validation | Must chain CSS, TypeScript, and custom audit scripts manually; Next.js CI is simpler because the framework handles more |

### Next.js Items Not Applicable to EJS

| Next.js Item | Why Not Applicable | EJS Replacement |
|-------------|-------------------|-----------------|
| #11 `useA11yCheck()` Hook | React hooks do not exist | #11 Playwright + axe-core audit script at build time |
| #14 `useBreakpoint()` Hook | No React hydration in EJS | CSS-only responsive via Tailwind prefixes; `window.matchMedia()` when unavoidable |
| #20 SSR Compatibility Guard (`isBrowser`) | EJS is always server-rendered; no hydration mismatch | #20 Escaped Output Policy — the analogous safety concern is `<%- %>` misuse |
| #27 Zustand Domain Store | Client-side state library with no React runtime | Express session + `req.session` for cross-request state; SSE (#42) for push |
| #38 React `<Suspense>` Skeleton | React 19 Suspense API | #37 `state: 'loading'` local passed to partials; SSR delivers full HTML so async skeletons are rarely needed |
| #43 Typed EventBus | Cross-component React event system | #42 Server-Sent Events for server→client; standard form POST for client→server |
